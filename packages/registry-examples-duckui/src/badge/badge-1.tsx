import { Badge } from '@gentleduck/registry-ui-duckui/badge'

export default function Badge1Demo() {
  return (
    <Badge arial-label="Badge" variant={'default'} size={'default'}>
      Badge
    </Badge>
  )
}
